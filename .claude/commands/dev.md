---
description: Start the Django development server
---

Start the Django development server for the EBM project.

1. Navigate to the Django project directory: `Scripts/EBM/`
2. Run `python manage.py runserver`
3. Report the URL where the server is accessible (usually http://127.0.0.1:8000/)
