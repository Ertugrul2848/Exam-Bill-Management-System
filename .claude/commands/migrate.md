---
description: Run Django database migrations
---

Run database migrations for the EBM project.

1. Navigate to `Scripts/EBM/`
2. Run `python manage.py makemigrations`
3. Review the migration files that were created
4. Run `python manage.py migrate`
5. Report what changes were applied to the database
