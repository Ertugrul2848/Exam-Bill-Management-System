---
description: Run Django system checks and validate the project
---

Run all Django system checks for the EBM project.

1. Navigate to `Scripts/EBM/`
2. Run `python manage.py check`
3. Run `python manage.py validate_templates` if available
4. Report any warnings or errors found
